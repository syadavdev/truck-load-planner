# SmartLoad Optimization API
## How to run
```bash
git clone https://github.com/syadavdev/truck-load-planner
cd truck-load-planner
docker compose up --build
# → Service will be available at http://localhost:8080